Ticket: Nightly report jobs crash on transient network errors because fetch_helper.get_json has no retry handling. Research the current best practice for stdlib-only retry with exponential backoff and jitter in Python (verify against current sources, not memory - our policy requires triangulated research for external claims), then add retry support to fetch_helper without breaking its two callers. Retries must apply only to transient failures, and the interactive sync path must not be slowed down by long retry waits.
